name: First WorkFlow
on:
  push:
    branches:
     - main

jobs:
  exmple-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Welcome Message
        run: echo "Welcome To This Workflow" 
      
      
